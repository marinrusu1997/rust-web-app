[package]
name = "lib-utils"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[lints]
workspace = true

[dependencies]
base64 = "0.22.1"
time = { version = "0.3.41", features = ["formatting", "parsing", "serde"] }
